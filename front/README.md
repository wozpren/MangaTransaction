# MangaTransaction Frontend

本目录包含 MangaTransaction 的 SvelteKit 前端。完整的安装、模型下载、OCR 服务和使用说明请参阅仓库根目录的 [`README.md`](../README.md)。

```bash
pnpm install
pnpm setup:model
pnpm dev
```

`pnpm dev` 需要先激活仓库根目录下的 Python 虚拟环境，并安装 `server/requirements.txt`。
